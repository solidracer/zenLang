# scope issue where locals wouldnt be popped properly.

while (true) {
    var x = 5;
    if (true) {
        var y = 3;
        break;
    }
    break;
}

print "exited loop!";

# if this code exits with code 0, the bug was fixed